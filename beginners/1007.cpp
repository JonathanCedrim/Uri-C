#include <iostream>

using std::cin;
using std::cout;
using std::endl;

int main() {
    int A, B, C, D, DIFFERENCE;

    cin >> A >> B >> C >> D;

    DIFFERENCE = (A * B) - (C * D);

    cout << "DIFERENCA = " << DIFFERENCE << endl;
}